package shapes

import (
	"github.com/miles-moran/catStax/points"
)

type Shape struct {
	rotations []points.PointArray
}

//Generates arrays of points based off of possible rotations of user input
func (shape *Shape) createShape(inputPoints points.Points) {

}
